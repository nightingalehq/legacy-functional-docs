---
title: "MMP0100 — generated tests (uipath)"
doc_type: generated_test
system: "MOM"
module: "MMP0100"
language: uipath
framework: testcase
generated_by: legacy-functional-docs 0.1.0
generated_at: "2026-08-11"
review_status: draft
reviewers: []
confidence_summary:
  verified: 6
  inferred: 0
  unresolved: 1
sources: ["MMP0100"]
---

# MMP0100 — generated tests (uipath / testcase)

Covers seven scenarios from the brief: the no-records-found escape (BR-001), the order-status confirmation check (BR-004), the grade-code mismatch escape (BR-007), the plant-code match check (BR-009), the two availability-tolerance branches (BR-011, BR-013), and the fallback WHEN NONE escape (BR-015). BR-009 has no reconstructable consequence in the fact brief, so its `then` step is marked `unresolved` rather than inventing an outcome. No `bug-desired` tests are present since every scenario in the brief is `characterization`. Dependencies (`MILL-ORDER`, `ORDER-AUDIT`, `STOCK-BALANCE` entities; `MMN0250`, `MMN0900` callees) are stubbed as preconditions only where the brief names them [[MMP0100]].

This is a UiPath Test Manager manual/data-driven test-case definition for
import -- not a UiPath Coded Test or a claim this tool can drive a 3270
screen end-to-end (it can't -- see
`docs/guides/testing-strategies-for-mainframes-and-4gl.md`). Real UiPath
Test Manager projects customize their test-case data schema; treat the
fields below as a first-draft mapping to adjust to your project's actual
schema, not a guaranteed-importable fixture. Stub the dependencies named
in the brief's "Dependencies to mock" section as Preconditions, using
only the values the brief actually states.

```yaml
# MMP0100:BR-001 [[MMP0100:34]]
# Branch: IF NO RECORDS FOUND no records found for preceding database loop
- test_case_id: "MMP0100-BR-001"
  title: "test_escapes_routine_when_no_records_found"
  preconditions:
    - "Stub MILL-ORDER entity per brief's Dependencies-to-mock list"
  steps:
    - given: "Preceding database loop returns no records"
      when: "IF NO RECORDS FOUND evaluates true"
      then: "#RETURN-CODE is set to 10 and the routine is escaped"
  status: characterization

# MMP0100:BR-004 [[MMP0100:38]]
# Branch: IF ORDER-VIEW.ORDER-STATUS NE 'CONF'
- test_case_id: "MMP0100-BR-004"
  title: "test_escapes_routine_when_order_status_not_confirmed"
  preconditions:
    - "Stub MILL-ORDER entity per brief's Dependencies-to-mock list"
  steps:
    - given: "ORDER-VIEW.ORDER-STATUS is not equal to 'CONF'"
      when: "IF ORDER-VIEW.ORDER-STATUS NE 'CONF' evaluates true"
      then: "#RETURN-CODE is set to 20 and the routine is escaped"
  status: characterization

# MMP0100:BR-007 [[MMP0100:44]]
# Branch: IF STOCK-VIEW.GRADE-CODE NE ORDER-VIEW.GRADE-CODE
- test_case_id: "MMP0100-BR-007"
  title: "test_escapes_bottom_when_grade_code_mismatch"
  preconditions:
    - "Stub STOCK-BALANCE entity per brief's Dependencies-to-mock list"
  steps:
    - given: "STOCK-VIEW.GRADE-CODE differs from ORDER-VIEW.GRADE-CODE"
      when: "IF STOCK-VIEW.GRADE-CODE NE ORDER-VIEW.GRADE-CODE evaluates true"
      then: "processing performs ESCAPE BOTTOM"
  status: characterization

# MMP0100:BR-009 [[MMP0100:47]]
# Branch: IF STOCK-VIEW.PLANT-CODE = #PLANT
- test_case_id: "MMP0100-BR-009"
  title: "test_plant_code_match_branch"
  preconditions:
    - "Stub STOCK-BALANCE entity per brief's Dependencies-to-mock list"
  steps:
    - given: "STOCK-VIEW.PLANT-CODE is set relative to #PLANT"
      when: "IF STOCK-VIEW.PLANT-CODE = #PLANT evaluates"
      then: "unresolved -- no reconstructable consequence in the fact brief"
  status: characterization

# MMP0100:BR-011 [[MMP0100:53]]
# Branch: WHEN #AVAIL-TOTAL >= ORDER-VIEW.ORDER-WEIGHT
- test_case_id: "MMP0100-BR-011"
  title: "test_order_status_released_when_availability_covers_full_weight"
  preconditions:
    - "Stub STOCK-BALANCE entity per brief's Dependencies-to-mock list"
  steps:
    - given: "#AVAIL-TOTAL is greater than or equal to ORDER-VIEW.ORDER-WEIGHT"
      when: "WHEN #AVAIL-TOTAL >= ORDER-VIEW.ORDER-WEIGHT evaluates true"
      then: "ORDER-VIEW.ORDER-STATUS is set to 'RLSD'"
  status: characterization

# MMP0100:BR-013 [[MMP0100:55]]
# Branch: WHEN #AVAIL-TOTAL >= ORDER-VIEW.ORDER-WEIGHT * (1 - #TOLERANCE-PCT / 100)
- test_case_id: "MMP0100-BR-013"
  title: "test_order_status_partial_when_availability_within_tolerance"
  preconditions:
    - "Stub STOCK-BALANCE entity per brief's Dependencies-to-mock list"
  steps:
    - given: "#AVAIL-TOTAL is greater than or equal to ORDER-VIEW.ORDER-WEIGHT * (1 - #TOLERANCE-PCT / 100), but less than ORDER-VIEW.ORDER-WEIGHT"
      when: "WHEN #AVAIL-TOTAL >= ORDER-VIEW.ORDER-WEIGHT * (1 - #TOLERANCE-PCT / 100) evaluates true"
      then: "ORDER-VIEW.ORDER-STATUS is set to 'PART'"
  status: characterization

# MMP0100:BR-015 [[MMP0100:58]]
# Branch: WHEN NONE
- test_case_id: "MMP0100-BR-015"
  title: "test_escapes_routine_when_no_availability_condition_matches"
  preconditions:
    - "Stub STOCK-BALANCE entity per brief's Dependencies-to-mock list"
  steps:
    - given: "Neither the BR-011 nor BR-013 availability condition is met"
      when: "WHEN NONE is reached"
      then: "#RETURN-CODE is set to 30 and the routine is escaped"
  status: characterization
```