---
title: "MMP0100 — generated tests (silkcentral)"
doc_type: generated_test
system: "MOM"
module: "MMP0100"
language: silkcentral
framework: testcase
generated_by: legacy-functional-docs 0.1.0
generated_at: "2026-08-11"
review_status: draft
reviewers: []
confidence_summary:
  verified: 6
  inferred: 0
  unresolved: 1
sources: ["MMP0100"]
---

# MMP0100 — generated tests (silkcentral / testcase)

Covers seven characterization scenarios cited in the brief: the no-records-found and non-`CONF` status escapes (BR-001, BR-004), the grade-code mismatch loop escape (BR-007), the two order-release status branches and their fallback `WHEN NONE` (BR-011, BR-013, BR-015). BR-009 (`STOCK-VIEW.PLANT-CODE = #PLANT`) is written up to the branch decision only — the brief has no reconstructable consequence for it, so the `then` step is marked `unresolved` rather than invented. [[MMP0100]]

This is a Silk Central test-*case definition* for import into that project's own case repository — not an executable SilkTest/4Test automation script, and not a claim that this tool can drive a 3270 screen (it can't — see `docs/guides/testing-strategies-for-mainframes-and-4gl.md`). Real Silk Central deployments customize their import field set per project; treat the fields below as a first-draft mapping to adjust to your project's actual Test Case template, not a guaranteed-importable fixture. Preconditions stub the entities/callees named in the brief's "Dependencies to mock" list (`MILL-ORDER`, `ORDER-AUDIT`, `STOCK-BALANCE`, `MMN0250`, `MMN0900`) using only the values the brief states.

```yaml
# MMP0100:BR-001 [[MMP0100:34]]
# Branch: IF NO RECORDS FOUND no records found for preceding database loop
- test_case_id: "MMP0100-BR-001"
  title: "test_order_lookup_no_records_found_sets_return_code_10"
  preconditions:
    - "Stub MILL-ORDER entity so the preceding database loop for #ORDER-NO/#PLANT returns no records"
  steps:
    - given: "MILL-ORDER holds no record matching the supplied #ORDER-NO and #PLANT"
      when: "the preceding database read loop completes with no records found"
      then: "#RETURN-CODE is set to 10 and the routine is escaped (ESCAPE ROUTINE)"
  status: characterization

# MMP0100:BR-004 [[MMP0100:38]]
# Branch: IF ORDER-VIEW.ORDER-STATUS NE 'CONF'
- test_case_id: "MMP0100-BR-004"
  title: "test_order_release_rejects_unconfirmed_order"
  preconditions:
    - "Stub MILL-ORDER entity so ORDER-VIEW.ORDER-STATUS is any value other than 'CONF'"
  steps:
    - given: "ORDER-VIEW.ORDER-STATUS is not equal to 'CONF'"
      when: "the status check IF ORDER-VIEW.ORDER-STATUS NE 'CONF' is evaluated"
      then: "#RETURN-CODE is set to 20 and the routine is escaped (ESCAPE ROUTINE)"
  status: characterization

# MMP0100:BR-007 [[MMP0100:44]]
# Branch: IF STOCK-VIEW.GRADE-CODE NE ORDER-VIEW.GRADE-CODE
- test_case_id: "MMP0100-BR-007"
  title: "test_stock_grade_mismatch_escapes_bottom_of_loop"
  preconditions:
    - "Stub STOCK-BALANCE entity so STOCK-VIEW.GRADE-CODE differs from ORDER-VIEW.GRADE-CODE"
  steps:
    - given: "STOCK-VIEW.GRADE-CODE is not equal to ORDER-VIEW.GRADE-CODE"
      when: "the condition IF STOCK-VIEW.GRADE-CODE NE ORDER-VIEW.GRADE-CODE is evaluated"
      then: "the current loop iteration is escaped (ESCAPE BOTTOM)"
  status: characterization

# MMP0100:BR-009 [[MMP0100:47]]
# Branch: IF STOCK-VIEW.PLANT-CODE = #PLANT
- test_case_id: "MMP0100-BR-009"
  title: "test_stock_plant_code_matches_parameter"
  preconditions:
    - "Stub STOCK-BALANCE entity so STOCK-VIEW.PLANT-CODE can be set equal to #PLANT"
  steps:
    - given: "STOCK-VIEW.PLANT-CODE is equal to #PLANT"
      when: "the condition IF STOCK-VIEW.PLANT-CODE = #PLANT is evaluated"
      then: "unresolved"
  status: characterization

# MMP0100:BR-011 [[MMP0100:53]]
# Branch: WHEN #AVAIL-TOTAL >= ORDER-VIEW.ORDER-WEIGHT
- test_case_id: "MMP0100-BR-011"
  title: "test_order_release_status_set_to_released_when_stock_sufficient"
  preconditions:
    - "Stub STOCK-BALANCE entity so accumulated #AVAIL-TOTAL is greater than or equal to ORDER-VIEW.ORDER-WEIGHT"
  steps:
    - given: "#AVAIL-TOTAL is greater than or equal to ORDER-VIEW.ORDER-WEIGHT"
      when: "the branch WHEN #AVAIL-TOTAL >= ORDER-VIEW.ORDER-WEIGHT is evaluated"
      then: "ORDER-VIEW.ORDER-STATUS is set to 'RLSD'"
  status: characterization

# MMP0100:BR-013 [[MMP0100:55]]
# Branch: WHEN #AVAIL-TOTAL >= ORDER-VIEW.ORDER-WEIGHT * (1 - #TOLERANCE-PCT / 100)
- test_case_id: "MMP0100-BR-013"
  title: "test_order_release_status_set_to_partial_within_tolerance"
  preconditions:
    - "Stub STOCK-BALANCE entity so #AVAIL-TOTAL falls below ORDER-VIEW.ORDER-WEIGHT but at or above the tolerance-adjusted threshold"
  steps:
    - given: "#AVAIL-TOTAL is less than ORDER-VIEW.ORDER-WEIGHT but greater than or equal to ORDER-VIEW.ORDER-WEIGHT * (1 - #TOLERANCE-PCT / 100)"
      when: "the branch WHEN #AVAIL-TOTAL >= ORDER-VIEW.ORDER-WEIGHT * (1 - #TOLERANCE-PCT / 100) is evaluated"
      then: "ORDER-VIEW.ORDER-STATUS is set to 'PART'"
  status: characterization

# MMP0100:BR-015 [[MMP0100:58]]
# Branch: WHEN NONE
- test_case_id: "MMP0100-BR-015"
  title: "test_order_release_falls_through_to_return_code_30"
  preconditions:
    - "Stub STOCK-BALANCE entity so #AVAIL-TOTAL is below both the full-weight and tolerance-adjusted thresholds"
  steps:
    - given: "#AVAIL-TOTAL is less than ORDER-VIEW.ORDER-WEIGHT * (1 - #TOLERANCE-PCT / 100)"
      when: "no prior WHEN branch matches and the DECIDE falls to WHEN NONE"
      then: "#RETURN-CODE is set to 30 and the routine is escaped (ESCAPE ROUTINE)"
  status: characterization
```