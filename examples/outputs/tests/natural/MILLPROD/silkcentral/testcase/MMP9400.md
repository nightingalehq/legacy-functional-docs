---
title: "MMP9400 — generated tests (silkcentral)"
doc_type: generated_test
system: "MOM"
module: "MMP9400"
language: silkcentral
framework: testcase
generated_by: legacy-functional-docs 0.1.0
generated_at: "2026-08-11"
review_status: draft
reviewers: []
confidence_summary:
  verified: 0
  inferred: 0
  unresolved: 1
sources: ["MMP9400"]
---

# MMP9400 — generated tests (silkcentral / testcase)

One scenario is covered: MMP9400:BR-002, the `IF #STATUS = 'CONF'` branch at [[MMP9400:11]]. The brief reports no reconstructable consequence for this branch, so the test case below stops at the branch decision and marks the expected outcome `unresolved` rather than inventing one. The named dependency `PROGA` [[MMP9400]] is stubbed as a precondition only; no call arguments or return shape are asserted since the brief does not state them. No `bug-desired` tests are included since the overlay does not report a defect for this scenario.

This is a Silk Central test-*case definition* for import into that
project's own case repository -- not an executable SilkTest/4Test
automation script, and not a claim that this tool can drive a 3270
screen (it can't -- see
`docs/guides/testing-strategies-for-mainframes-and-4gl.md`). Real Silk
Central deployments customize their import field set per project; treat
the fields below as a first-draft mapping to adjust to your project's
actual Test Case template, not a guaranteed-importable fixture. Stub the
dependencies named in the brief's "Dependencies to mock" section as
Preconditions, using only the values the brief actually states.

```yaml
# MMP9400:BR-002 [[MMP9400:11]]
# Branch: IF #STATUS = 'CONF'
- test_case_id: "MMP9400-BR-002"
  title: "test_status_conf_branch_decision"
  preconditions:
    - "Stub callee PROGA per brief's Dependencies-to-mock list"
  steps:
    - given: "#STATUS is set such that the condition #STATUS = 'CONF' can be evaluated"
      when: "MMP9400 evaluates IF #STATUS = 'CONF'"
      then: "unresolved -- no reconstructable consequence in the cited source excerpt"
  status: characterization
```