---
title: "MMP9000 — generated tests (mantis)"
doc_type: generated_test
system: "MOM"
module: "MMP9000"
language: mantis
framework: native
generated_by: legacy-functional-docs 0.1.0
generated_at: "2026-08-11"
review_status: draft
reviewers: []
confidence_summary:
  verified: 1
  inferred: 0
  unresolved: 0
sources: ["MMP9000"]
---

# MMP9000 — generated tests (mantis / native)

Covers one scenario, MMP9000:BR-001 [[MMP9000:14]], characterizing the branch that sets `#FLAG` to `1` when the order is confirmed for customer `C00123`. The brief lists no "Dependencies to mock" section for this member, so no stubs are set up beyond the fixture record fields the condition itself references.

No dedicated Mantis unit-test framework exists, so this is a native
driver program: one paragraph per scenario that sets up fixture input,
`PERFORM`s the paragraph/subroutine under test, and compares actual vs.
expected with an `IF`/`DISPLAY` pair -- run as a batch job in the same
Mantis/Supra environment the module itself runs in, read by eye or piped
through `grep FAIL`.

See [`MMP9000.mantis`](./MMP9000.mantis) for the generated test source.

## Scenarios covered

- MMP9000:BR-001
