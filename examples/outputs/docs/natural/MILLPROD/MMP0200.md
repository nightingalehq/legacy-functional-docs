---
title: "MMP0200 — Mill certificate enquiry (online)"
doc_type: module
system: "MOM"
module: "MMP0200"
dialect: "natural"
library: "MILLPROD"
object_type: "program"
generated_by: legacy-functional-docs 0.1.0
generated_at: "2026-08-11"
review_status: draft
reviewers: []
confidence_summary:
  verified: 9
  inferred: 2
  unresolved: 5
sources: ["MMP0200"]
sme_questions:
  - "Does control ever return to the CALL PDFGEN statement at MMP0200:23 after the FETCH RETURN at MMP0200:22, or is that CALL unreachable?"
  - "What does PDFGEN do with the certificate number, and where is its source documented?"
  - "What fields and prompts does the MMM0200 screen map present to the user?"
  - "What does the *ERROR-NR value written at MMP0200:25 mean to an operator, and what should they do in response?"
  - "Is a commit expected anywhere in the certificate enquiry transaction, given MMP0200 itself performs no database writes?"
---

# MMP0200 — Mill certificate enquiry (online)

## Purpose

MMP0200 looks up a mill certificate (`MILL-CERT`) by certificate number [[MMP0200:15]] and displays the heat number (`HEAT-NO`) and cast date (`CAST-DATE`) for the matching record [[MMP0200:19]]. The module's header comment describes it as an online certificate enquiry program [[MMP0200:1]] *(inferred — header comments are unverified author prose, but this description is consistent with the FIND and DISPLAY statements found in the source)*.

## How it is invoked

MMP0200 is called from `STEEL` [[STEEL:2]]. On entry it takes user input through a screen map named `MMM0200` [[MMP0200:11]]. The source for `MMM0200` was not supplied *(unresolved — the map's field layout and prompt text cannot be confirmed from the ingested source; see gaps)*.

## Inputs

| Name | Format | Source | Notes | Citation |
|---|---|---|---|---|
| `#CERT-NO` | not specified in supplied source | Entered by the user via the `MMM0200` screen map | Field definition not available because `MMM0200`'s source was not supplied | [[MMP0200:11]] |

## Data used

| Data store | Operations | Key / access path | Purpose | Citation |
|---|---|---|---|---|
| `MILL-CERT` (via `CERT-VIEW`) | FIND (read) | `CERT-NO` descriptor, `WITH CERT-NO = #CERT-NO` | Retrieves the certificate record matching the certificate number the user entered, to obtain its heat number and cast date | [[MMP0200:15]] |

## Business rules

1. **Required entry check** (MMP0200:BR-001) — If the entered certificate number (`#CERT-NO`) is blank, the module re-displays the input screen with the message "Certificate number is required" rather than proceeding to look up a certificate [[MMP0200:12-13]].
2. **No matching certificate** (MMP0200:BR-002) — If the `FIND` on `MILL-CERT` returns no records, the module re-displays the input screen with the message "No certificate found for that number" [[MMP0200:16-17]].
3. **Transfer to MMP0300** (MMP0200:BR-003) — `#PGM` is set to the literal `'MMP0300'` [[MMP0200:21]], and `FETCH RETURN #PGM` then transfers control to that program [[MMP0200:22]]. Whether execution can subsequently reach the `CALL PDFGEN` statement immediately following is not determinable from the source alone *(inferred — `FETCH RETURN` conventionally transfers control without returning; unresolved whether the following statement is therefore unreachable, see gaps)*.
4. **Error handling on the enquiry** (MMP0200:BR-004) — An `ON ERROR` block is defined for the module [[MMP0200:24]], and on error the module writes the value of `*ERROR-NR` [[MMP0200:25]].

## Processing sequence

1. Control is received from `STEEL` [[STEEL:2]].
2. The user enters input via the `MMM0200` map [[MMP0200:11]].
3. If `#CERT-NO` is blank, the screen is re-input with "Certificate number is required" [[MMP0200:12-13]].
4. `MILL-CERT` is searched with `CERT-NO = #CERT-NO` [[MMP0200:15]].
5. If no records are found, the screen is re-input with "No certificate found for that number" [[MMP0200:16-17]].
6. `CERT-VIEW.HEAT-NO` and `CERT-VIEW.CAST-DATE` are displayed [[MMP0200:19]].
7. `#PGM` is set to `'MMP0300'` [[MMP0200:21]].
8. `FETCH RETURN #PGM` transfers control [[MMP0200:22]].
9. `CALL PDFGEN` is coded next, passing `#CERT-NO` [[MMP0200:23]]; whether this statement is ever reached is unresolved (see Business rule 3 above and gaps).
10. On error, `*ERROR-NR` is written [[MMP0200:24-25]].

## Transaction boundaries

No commit (`END TRANSACTION`) statement was found in this module's source *(unresolved — MMP0200 performs only a read (`FIND`) against `MILL-CERT` in the supplied source, so a commit may not be expected here, but this should be confirmed with an SME familiar with the enquiry transaction's design)*.

## Outputs and effects

- The heat number and cast date for the matched certificate are displayed to the user [[MMP0200:19]].
- Control is transferred to `MMP0300` via `FETCH RETURN` [[MMP0200:22]].
- `PDFGEN`, a non-Natural (3GL) module, is called with `#CERT-NO` [[MMP0200:23]]; its behaviour is outside the scanned source and its source was not supplied *(unresolved — see gaps)*.
- On error, the value of `*ERROR-NR` is written [[MMP0200:25]].

## Error handling

- Blank certificate number: re-input with "Certificate number is required" [[MMP0200:12-13]].
- No matching certificate found: re-input with "No certificate found for that number" [[MMP0200:16-17]].
- `ON ERROR` block: writes `*ERROR-NR` [[MMP0200:24-25]]; what value(s) `*ERROR-NR` can take and what an operator should do in response are not specified in the supplied source *(unresolved — see gaps)*.

## Gaps and questions for review

1. The `MMM0200` screen map's source was not supplied [[MMP0200:11]] — what fields and prompts does this screen present to the user?
2. `FETCH RETURN` at [[MMP0200:22]] transfers to the program named in `#PGM`, which was just set to `MMP0300` [[MMP0200:21]] — does control ever return afterward to execute the `CALL PDFGEN` statement at [[MMP0200:23]], or is that call unreachable?
3. `CALL PDFGEN` at [[MMP0200:23]] invokes a non-Natural (3GL) module whose source was not supplied — what does `PDFGEN` do with the certificate number, and where is its source documented?
4. What does the `*ERROR-NR` value written at [[MMP0200:25]] mean to an operator, and what should they do in response?
5. Is a commit expected anywhere in the certificate enquiry transaction, given that MMP0200 itself performs no database writes in the supplied source?