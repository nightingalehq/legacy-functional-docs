---
title: "MMP9000 — Continuation-fold regression fixture flagging confirmed orders for customer C00123"
doc_type: module
system: "MOM"
module: "MMP9000"
dialect: "natural"
library: "MILLPROD"
object_type: "program"
generated_by: legacy-functional-docs 0.1.0
generated_at: "2026-08-11"
review_status: draft
reviewers: []
confidence_summary:
  verified: 5
  inferred: 3
  unresolved: 3
sources: ["MMP9000", "MILL-ORDER"]
sme_questions:
  - "Is MMP9000 test/fixture code, or does it perform a real business check on confirmed orders for customer C00123?"
  - "What does statement [[MMP9000:15]] actually say, and does it change the condition evaluated at [[MMP9000:14]]?"
  - "What does the program do with `#FLAG` after it is set at [[MMP9000:16]], and is that logic present elsewhere or missing from the supplied source?"
  - "Is MMP9000 invoked from a job step, CICS transaction, menu, or scheduler not included in the supplied source, or is it dead/unused code?"
---

# MMP9000 — Continuation-fold regression fixture flagging confirmed orders for customer C00123

## Purpose

The module declares a view `ORDER-VIEW` over the mill order (`MILL-ORDER`) [[MMP9000:7]] and checks whether a specific mill order's status is `CONF` (confirmed) for customer `C00123`, setting a flag when it is [[MMP9000:14]][[MMP9000:16]]. The header comments describe this module as a regression fixture for a source-scanning edge case rather than production functionality *(inferred from header comment [[MMP9000:1-5]] — treat as an unverified author claim, not a confirmed fact; no other code in the module confirms or contradicts it)*.

## How it is invoked

No JCL step, CICS transaction, or program call referencing MMP9000 was found in the supplied source [[MMP9000]]. It may be dead code, invoked dynamically, or started from a scheduler or menu definition not included in this engagement's inputs *(unresolved — needs SME confirmation of how, or whether, MMP9000 is actually invoked)*.

## Inputs

| Name | Format | Source | Notes | Citation |
|---|---|---|---|---|
| `ORDER-VIEW` | Adabas view | `MILL-ORDER` | View declared over the mill order to read its status and customer number | [[MMP9000:7]] |

## Data used

| Data store | Operations | Key / access path | Purpose | Citation |
|---|---|---|---|---|
| `MILL-ORDER` (via `ORDER-VIEW`) | Read (fields referenced in condition) | `ORDER-VIEW.ORDER-STATUS`, `ORDER-VIEW.CUSTOMER-NO` | Determine whether a mill order is confirmed for customer C00123 | [[MMP9000:14]] |

No read/write access beyond the fields referenced in the condition was found in this module.

## Business rules

1. **Confirmed order flag for customer C00123** (MMP9000:BR-001) — When a mill order's status is `CONF` (confirmed) and its customer number is `C00123`, the program sets `#FLAG` to `1` [[MMP9000:14]] (MMP9000:BR-002) [[MMP9000:16]]. *(inferred — the statement immediately following the condition, [[MMP9000:15]], did not parse and is recorded as a gap; the header comments state this program specifically exercises a case where a condition wraps onto the next line before a connective such as `AND`/`OR`, "the truncated version still looks like a complete, well-formed rule" [[MMP9000:2-5]]. This raises the possibility that the condition actually evaluated by the source includes additional terms on line 15 that are not reflected in the condition captured above — confirm with the source and an SME before treating [[MMP9000:14]] as the complete rule.)*

## Processing sequence

1. Evaluate whether `ORDER-VIEW.ORDER-STATUS = 'CONF'` and `ORDER-VIEW.CUSTOMER-NO = 'C00123'` [[MMP9000:14]].
2. An unparsed statement follows the condition; its content and effect on the branch, if any, could not be determined by the scanner [[MMP9000:15]] *(unresolved)*.
3. Within the branch, `#FLAG` is set to `1` [[MMP9000:16]].

No further processing was found in the 22-line source [[MMP9000]].

## Transaction boundaries

No commit (`END TRANSACTION`) or other database-write statement was found in this module [[MMP9000]]. If MMP9000 is more than a test fixture, confirm with an SME whether it performs any persistent update, and if so, where that commit occurs *(unresolved)*.

## Outputs and effects

The only effect found in the supplied source is setting the local variable `#FLAG` to `1` [[MMP9000:16]]. No screen output, message, file write, or called module was found. What downstream logic consumes `#FLAG`, if any, is not present in this module *(unresolved)*.

## Error handling

No error-handling statements (e.g. `REINPUT`, message issuance) were found in this module [[MMP9000]].

## Gaps and questions for review

- No JCL step, CICS transaction, or program call refers to MMP9000. Is it dead code, invoked dynamically, or started some other way not present in the supplied source? [[MMP9000]]
- The statement at [[MMP9000:15]] was not recognised by the Natural scanner. What does it contain, and does it extend or alter the condition at [[MMP9000:14]]? [[MMP9000:15]]