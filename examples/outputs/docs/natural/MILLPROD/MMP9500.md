---
title: "MMP9500 — Report output of heat number and cast date (fixture status unconfirmed)"
doc_type: module
system: "MOM"
module: "MMP9500"
dialect: "natural"
library: "MILLPROD"
object_type: "program"
generated_by: legacy-functional-docs 0.1.0
generated_at: "2026-08-11"
review_status: draft
reviewers: []
confidence_summary:
  verified: 1
  inferred: 1
  unresolved: 3
sources: ["MMP9500"]
sme_questions:
  - "Is MMP9500 live functionality, or is it a test/development fixture as its own header comments suggest? No JCL step, CICS transaction, or program call to it was found in the supplied source."
  - "What do the statements at MMP9500:14 and MMP9500:15 do? The Natural scanner could not recognise them."
---

# MMP9500 — Report output of heat number and cast date (fixture status unconfirmed)

## Purpose

The module contains a `WRITE` statement that outputs the heat number
(`HEAT-NO`) and cast date (`CAST-DATE`) at fixed report columns 5 and 30
[[MMP9500:13]]. Beyond this single statement, the module's header comments
describe it as a fixture built to exercise how the Natural scanner handles
report-writer column-specification continuation lines (`5T`/`30T`), rather
than as production reporting logic *(inferred from header comments
[[MMP9500:1-8]] — header comments are unverified author prose and are not
confirmed by any other cited behaviour in this module)*.

## How it is invoked

No JCL step, CICS transaction, or program call referring to MMP9500 was
found in the supplied source. It may be dead code, invoked dynamically, or
started from a scheduler or menu definition that was not supplied
*(unresolved — needs SME confirmation of whether and how MMP9500 is
invoked)* [[MMP9500]].

## Inputs

| Name | Format | Source | Notes | Citation |
|---|---|---|---|---|

No parameter or context-setting input facts were supplied for this module.

## Data used

| Data store | Operations | Key / access path | Purpose | Citation |
|---|---|---|---|---|

No data store access facts were supplied for this module.

## Business rules

No business rule candidates were identified in the brief for this module.

## Processing sequence

1. The module writes a report line containing the heat number at column 5
   and the cast date at column 30 [[MMP9500:13]].
2. Statements at lines 14 and 15 could not be parsed by the Natural scanner,
   so their effect on processing is not known *(unresolved — needs source
   review to determine what these lines do)* [[MMP9500:14]] [[MMP9500:15]].

## Transaction boundaries

No commit (`END TRANSACTION`) or other transaction-boundary statement was
found in this module. Whether any database changes occur here, and if so
how they are committed, needs confirmation *(unresolved)*.

## Outputs and effects

- A report line showing the heat number and cast date, written to the
  report/print destination via the `WRITE` statement [[MMP9500:13]].

## Error handling

No error-handling, `REINPUT`, or user-facing message statements were
supplied for this module.

## Gaps and questions for review

1. Is MMP9500 actually invoked anywhere in this system — no JCL, CICS, or
   program call to it was found — or is it dead code, a dynamically-invoked
   module, or a test/development fixture as its own header comments suggest?
   [[MMP9500]]
2. What do the unrecognised statements at MMP9500:14 do? [[MMP9500:14]]
3. What do the unrecognised statements at MMP9500:15 do? [[MMP9500:15]]