---
title: "MMP9200 — Loop-label resolution regression fixture"
doc_type: module
system: "MOM"
module: "MMP9200"
dialect: "natural"
library: "MILLPROD"
object_type: "program"
generated_by: legacy-functional-docs 0.1.0
generated_at: "2026-08-11"
review_status: draft
reviewers: []
confidence_summary:
  verified: 8
  inferred: 2
  unresolved: 5
sources: ["MMP9200"]
sme_questions:
  - "Is MMP9200 dead code, invoked dynamically, or started from a scheduler or menu definition not supplied? No JCL step, CICS transaction, or program call referring to it was found."
  - "What store does the DELETE (X9.) statement at MMP9200:16 actually target, given no FIND loop in this module opens a label X9.?"
  - "MMP9200 performs an UPDATE and a DELETE with no END TRANSACTION / COMMIT found — is commit handled by a caller, by the TP monitor at task end, or not at all?"
---

# MMP9200 — Loop-label resolution regression fixture

## Purpose

Per its own header comments, this module is a regression fixture for loop-label
resolution rather than a production business module *(inferred from
[[MMP9200:1-5]])*. It opens a labelled FIND loop over the mill order
(`MILL-ORDER`) view and exercises whether an `UPDATE` statement referencing that
loop's label correctly resolves to the same store, while a `DELETE` statement
referencing a label nothing opened is meant to remain an honest unresolved gap
*(inferred from [[MMP9200:2-5]])*.

## How it is invoked

No JCL step, CICS transaction, or program call referring to MMP9200 was found in
the supplied source *(unresolved — confirm whether this is dead code, a
dynamically invoked module, or started from a scheduler or menu definition not
supplied)* [[MMP9200]].

## Inputs

| Name | Format | Source | Notes | Citation |
|---|---|---|---|---|
| — | — | — | No parameters were declared in the 18 lines of source supplied for this module. | [[MMP9200]] |

## Data used

| Data store | Operations | Key / access path | Purpose | Citation |
|---|---|---|---|---|
| mill order (`MILL-ORDER`, via `ORDER-VIEW`) | Read (FIND) | descriptor `ORDER-STATUS` | Selects mill order records by status for processing in the `F1.` loop. | [[MMP9200:7]] [[MMP9200:12]] |
| mill order (`MILL-ORDER`, via `ORDER-VIEW`) | Update | resolved via the loop label `UPDATE (F1.)`, which updates the record currently held by the `F1.` FIND loop | Updates the mill order record currently being processed in the loop. | [[MMP9200:13]] |
| store not determinable | Delete | label `X9.` | Not determinable *(unresolved — no FIND loop in this module opens a label `X9.`, so the target store cannot be confirmed)*. | [[MMP9200:16]] |

## Business rules

No conditional business logic (`IF`/`DECIDE` branching) was found in this
module's 18 lines of source; the only operations present are the read, update,
and delete listed above [[MMP9200]].

## Processing sequence

1. Open a FIND loop over the mill order (`MILL-ORDER`, via `ORDER-VIEW`),
   selecting records by `ORDER-STATUS` [[MMP9200:12]].
2. Within that loop, update the current mill order record via the `UPDATE
   (F1.)` statement, which resolves to the same `MILL-ORDER` store opened by
   the loop [[MMP9200:13]].
3. Delete a record via the `DELETE (X9.)` statement; the store this targets
   cannot be determined because no FIND loop labelled `X9.` exists in this
   module *(unresolved)* [[MMP9200:16]].

## Transaction boundaries

No `END TRANSACTION` or `COMMIT` statement was found in this module, despite it
performing an update and a delete *(unresolved — confirm whether commit is
handled by a caller, by the TP monitor at task end, or not at all)* [[MMP9200]].

## Outputs and effects

The module updates a mill order record reached via the `F1.` loop label
[[MMP9200:13]], and deletes a record reached via the `X9.` label whose target
store is unresolved *(unresolved — see Data used)* [[MMP9200:16]].

## Error handling

No error handling (`REINPUT` or message text) was found in this module's source
[[MMP9200]].

## Gaps and questions for review

1. Is MMP9200 dead code, invoked dynamically, or started from a scheduler or
   menu definition not supplied? No caller was found in the supplied source
   [[MMP9200]].
2. What store does the `DELETE (X9.)` statement at line 16 actually target,
   given no FIND loop in this module opens a label `X9.`? [[MMP9200:16]]
3. MMP9200 performs an update and a delete with no `END TRANSACTION` /
   `COMMIT` found — is commit handled by a caller, by the TP monitor at task
   end, or not at all? [[MMP9200]]