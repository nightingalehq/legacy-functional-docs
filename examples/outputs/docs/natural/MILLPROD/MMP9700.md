---
title: "MMP9700 — Mill order lookup by steel grade (purpose unconfirmed)"
doc_type: module
system: "MOM"
module: "MMP9700"
dialect: "natural"
library: "MILLPROD"
object_type: "program"
generated_by: legacy-functional-docs 0.1.0
generated_at: "2026-08-11"
review_status: draft
reviewers: []
confidence_summary:
  verified: 2
  inferred: 1
  unresolved: 7
sources: ["MMP9700"]
sme_questions:
  - "Is MMP9700 still invoked anywhere (batch job step, CICS transaction, menu, or scheduler), or is it dead code?"
  - "What do the statements at MMP9700:8 and MMP9700:10 do? The Natural scanner could not parse either line."
  - "MMP9700 is written in Natural reporting mode, where loop/condition nesting depends on implicit block scope rather than explicit END markers. Can an SME confirm the intended nesting so the processing sequence can be documented reliably?"
  - "Is there any business rule logic in MMP9700 beyond the mill order lookup, particularly in the two unparsed lines?"
---

# MMP9700 — Mill order lookup by steel grade (purpose unconfirmed)

## Purpose

The module appears to look up a mill order (`MILL-ORDER`) by steel grade (`GRADE-CODE`) and display the resulting order number *(inferred, from combining [[MMP9700:7]] and [[MMP9700:9]] — no header comment or surrounding call context confirms this is the module's actual business purpose)*. The supplied header comments describe MMP9700 as a test fixture for a documentation-tooling defect ("AMBIGUOUS REPORTING-MODE NESTING FIXTURE") rather than as production logic [[MMP9700:1-6]]; per the writing rules, header comments are unverified author prose and are not used here as evidence of business purpose, but the discrepancy itself is worth raising with an SME *(unresolved — see gaps)*.

## How it is invoked

No JCL step, CICS transaction, or program call refers to MMP9700 in the supplied source *(unresolved — [[MMP9700]] orphan_module gap; it may be dead code, invoked dynamically, or started from a scheduler or menu definition not supplied)*.

## Inputs

| Name | Format | Source | Notes | Citation |
|---|---|---|---|---|
| `GRADE-CODE` | Not specified in supplied source | Used as the search key on the `MILL-ORDER` descriptor | Exact format/length not determinable from the statement shown *(unresolved)* | [[MMP9700:7]] |

## Data used

| Data store | Operations | Key / access path | Purpose | Citation |
|---|---|---|---|---|
| mill order (`MILL-ORDER`) | `FIND` (read) | `GRADE-CODE` descriptor, `WITH GRADE-CODE = ...` | Retrieves the mill order(s) matching a given steel grade *(inferred — the retrieved order is subsequently displayed by order number)* | [[MMP9700:7]] |

## Business rules

No business rule conditions (e.g. `IF`/`DECIDE` logic) were found in the supplied source for this module *(unresolved — the brief contains no rule_candidate facts for MMP9700; it is not known whether such logic exists in the two unparsed lines, [[MMP9700:8]] and [[MMP9700:10]])*.

## Processing sequence

1. The module finds mill order records matching the supplied steel grade [[MMP9700:7]].
2. A statement follows that the Natural scanner could not parse; its effect on control flow is unknown *(unresolved)* [[MMP9700:8]].
3. The module displays the order number (`ORDER-NO`) [[MMP9700:9]].
4. A further statement follows that the Natural scanner could not parse; its effect on control flow is unknown *(unresolved)* [[MMP9700:10]].

Because MMP9700 is written in Natural reporting mode, where a `LOOP` body is not distinguished from the `LOOP` line by indentation, the actual nesting of these four statements (e.g. whether the `DISPLAY` sits inside a loop over `MILL-ORDER` finds) cannot be reliably inferred from source layout alone *(unresolved — [[MMP9700]] reporting_mode gap)*.

## Transaction boundaries

No commit (`END TRANSACTION` or equivalent) was found in the supplied source for this module *(unresolved — confirm with an SME whether MMP9700 performs only reads, or whether commit handling occurs in a caller not supplied)*.

## Outputs and effects

The order number (`ORDER-NO`) of the found mill order is displayed to the user [[MMP9700:9]].

## Error handling

No error-handling logic (e.g. `NOREC`, `REINPUT`) was found in the supplied source for this module *(unresolved — it is not known what happens if no `MILL-ORDER` record matches the supplied grade code)*.

## Gaps and questions for review

1. Is MMP9700 still invoked anywhere — a batch job step, CICS transaction, menu, or scheduler entry — or is it dead code? [[MMP9700]]
2. What does the statement at line 8 do? The Natural scanner could not parse it. [[MMP9700:8]]
3. What does the statement at line 10 do? The Natural scanner could not parse it. [[MMP9700:10]]
4. MMP9700 is written in Natural reporting mode, where loop and condition nesting is implicit rather than marked with explicit `END` statements. Can an SME confirm the intended nesting of the `FIND`, `DISPLAY`, and the two unparsed statements? [[MMP9700]]