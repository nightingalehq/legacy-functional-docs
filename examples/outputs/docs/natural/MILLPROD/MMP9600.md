---
title: "MMP9600 — Mill order lookup and display by steel grade"
doc_type: module
system: "MOM"
module: "MMP9600"
dialect: "natural"
library: "MILLPROD"
object_type: "program"
generated_by: legacy-functional-docs 0.1.0
generated_at: "2026-08-11"
review_status: draft
reviewers: []
confidence_summary:
  verified: 2
  inferred: 2
  unresolved: 3
sources: ["MMP9600"]
sme_questions:
  - "Should the LOOP structure in MMP9600, inferred from indentation rather than a structured-mode terminator, be confirmed against an actual program listing before it is treated as verified logic?"
  - "Is MMP9600 live functionality — invoked dynamically, from a scheduler, or from a menu definition not supplied — or is it dead/orphan code? No JCL step, CICS transaction, or program call referencing it was found."
  - "What does the unrecognised statement at MMP9600:12 do?"
---

# MMP9600 — Mill order lookup and display by steel grade

## Purpose

Based on the statements present, the module finds a mill order (`MILL-ORDER`) by steel grade (`GRADE-CODE`) [[MMP9600:8]] and displays the order number [[MMP9600:10]] *(inferred — the sequence of a keyed FIND followed by a DISPLAY of ORDER-NO suggests a simple order-lookup/reporting purpose; no explicit statement of intent was found)*.

The module's header comments describe it instead as a fixture built to test this documentation tool's own block-inference logic ("REPORTING-MODE BLOCK INFERENCE FIXTURE (ISSUE #5)") [[MMP9600:1-7]]. This contradicts treating the module as representing real business functionality, and header comments are unverified author prose rather than fact — this discrepancy is recorded as a gap rather than resolved either way; see "Gaps and questions for review."

## How it is invoked

No JCL step, CICS transaction, or program call referring to MMP9600 was found in the supplied source [[MMP9600]] *(unresolved — it may be dead code, invoked dynamically, or started from a scheduler or menu definition not supplied; needs SME confirmation)*.

## Inputs

| Name | Format | Source | Notes | Citation |
|---|---|---|---|---|
| `GRADE-CODE` | steel grade (business term) | Used as search key against `MILL-ORDER` | Value supplied to the FIND's WITH clause; the value's own origin (parameter, prior read, or literal) is not shown in the captured statement | [[MMP9600:8]] |

## Data used

| Data store | Operations | Key / access path | Purpose | Citation |
|---|---|---|---|---|
| `MILL-ORDER` (mill order) | FIND (read) | descriptor `GRADE-CODE` (an indexed field the database can search on), `WITH GRADE-CODE = ` | Locates the mill order(s) matching a given steel grade | [[MMP9600:8]] |

## Business rules

1. **Iteration over found mill orders** (MMP9600:BR-001) — the module repeats a block of logic once a mill order matching the grade code has been found [[MMP9600:9]] *(inferred — the LOOP's presence and nesting were inferred from indentation depth rather than a structured-mode terminator, because reporting-mode Natural has no explicit end-of-loop marker in this source; the brief records this candidate rule's confidence as `inferred`. Confirm against a real listing before treating this as verified structure.)*

## Processing sequence

1. Find a mill order by steel grade [[MMP9600:8]].
2. Loop over the result *(inferred structure)* [[MMP9600:9]].
3. Display the order number [[MMP9600:10]].

No further steps were captured in the supplied source; one statement at line 12 was not recognised by the scanner (see "Gaps and questions for review").

## Transaction boundaries

No commit (END TRANSACTION) statement was found in this module [[MMP9600]] *(unresolved — needs SME confirmation of whether this module changes data at all, and if so where any commit occurs)*.

## Outputs and effects

- Displays the order number to the user [[MMP9600:10]].

No other outputs, messages, or invoked modules were found in the supplied source.

## Error handling

No error-handling or REINPUT statements were found in the supplied source for this module.

## Gaps and questions for review

1. LOOP nesting in this module was inferred purely from indentation, because no structured-mode terminator is present anywhere in the source; every LOOP's body was consistently more indented than the LOOP line itself. Can this be confirmed against a real listing before it is treated as verified structure? [[MMP9600]]
2. No JCL step, CICS transaction, or program call refers to MMP9600. Is it dead code, invoked dynamically, or started from a scheduler or menu definition that was not supplied? [[MMP9600]]
3. The statement at line 12 was not recognised by the Natural scanner. What does it do? [[MMP9600:12]]