---
title: "MMC0100 — Shared steel grade validation"
doc_type: module
system: "MOM"
module: "MMC0100"
dialect: "natural"
library: "MILLPROD"
object_type: "copycode"
generated_by: legacy-functional-docs 0.1.0
generated_at: "2026-08-11"
review_status: draft
reviewers: []
confidence_summary:
  verified: 3
  inferred: 1
  unresolved: 2
sources: ["MMC0100"]
sme_questions:
  - "What does a #VALIDATION-RC value of 99 signify to the calling program, and what values indicate success or other failure modes?"
  - "What happens when #GRADE-CODE is not 'X9' — is there an ELSE branch or further validation in the lines not captured in this brief (MMC0100 lines 4-5)?"
---

# MMC0100 — Shared steel grade validation

## Purpose

This copycode (a Natural source fragment included into another program's source at compile time, rather than called at runtime) checks a steel grade (`GRADE-CODE`) value and sets a return code when the grade matches `'X9'` [[MMC0100:2-3]]. It appears intended as shared validation logic reused across programs, per its header comment "SHARED GRADE VALIDATION COPYCODE" [[MMC0100:1]], though that comment is unverified author prose and the brief does not show other consumers besides the one caller identified below. *(inferred — the specific business purpose of flagging grade `'X9'` is not stated anywhere in the supplied source.)*

## How it is invoked

`MMP9100` includes this copycode via `INCLUDE MMC0100` [[MMP9100:11]]. Because it is a copycode rather than a callable module, its statements execute inline as part of `MMP9100`'s own source at the point of inclusion; no other callers were found in the supplied source.

## Inputs

| Name | Format | Source | Notes | Citation |
|---|---|---|---|---|
| `#GRADE-CODE` | Natural user-defined variable | Set by the including program before this copycode's logic runs | Holds the steel grade (`GRADE-CODE`) being validated; declaration not shown in this member's source | [[MMC0100:2]] |

## Data used

No database or file access was found in this member's source.

## Business rules

1. **Grade code X9 sets validation return code 99** (MMC0100:BR-001, MMC0100:BR-002) — When `#GRADE-CODE` equals `'X9'` [[MMC0100:2]], the copycode moves `99` into `#VALIDATION-RC` [[MMC0100:3]]. *(The business meaning of return code 99, and what the including program does with it, is not defined in the supplied source — unresolved.)*

## Processing sequence

1. Evaluate whether `#GRADE-CODE` equals `'X9'` [[MMC0100:2]].
2. If true, set `#VALIDATION-RC` to `99` [[MMC0100:3]].
3. *(unresolved — the brief's line count for this member is 5, but only lines 1-3 appear in the supplied facts; the behaviour of lines 4-5, including any ELSE handling, is not determinable from this brief.)*

## Transaction boundaries

No commit or database write was found in this member's source; this copycode performs no persistence itself.

## Outputs and effects

Sets `#VALIDATION-RC` to `99` when the input condition is met [[MMC0100:3]]. No other outputs, messages, or side effects were found in the supplied source.

## Error handling

No `REINPUT`, message text, or other error-handling statement was found in the supplied source for this member.

## Gaps and questions for review

1. What does a `#VALIDATION-RC` value of `99` signify to the calling program, and what other values does this field take on success or other failure paths?
2. What happens when `#GRADE-CODE` is not `'X9'` — is there an ELSE branch or additional logic in lines 4-5 of `MMC0100` that was not included in this brief?