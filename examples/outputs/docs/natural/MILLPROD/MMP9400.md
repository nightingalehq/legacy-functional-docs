---
title: "MMP9400 — Regression fixture for labelled-statement scanning (no confirmed business function)"
doc_type: module
system: "MOM"
module: "MMP9400"
dialect: "natural"
library: "MILLPROD"
object_type: "program"
generated_by: legacy-functional-docs 0.1.0
generated_at: "2026-08-11"
review_status: draft
reviewers: []
confidence_summary:
  verified: 6
  inferred: 1
  unresolved: 4
sources: ["MMP9400"]
sme_questions:
  - "Is MMP9400 live functionality, dead code, or a test/regression fixture? No JCL step, CICS transaction, or program call referencing it was found."
  - "What statement was intended at line 13? It was not recognised by the Natural scanner."
  - "Is source for CALLNAT target PROGA available, and what does it do?"
  - "What action is intended when #STATUS equals CONF (the condition tested at line 11)? No corresponding statement body was captured."
---

# MMP9400 — Regression fixture for labelled-statement scanning

## Purpose

The module's header comments describe it as a "labelled statement regression fixture" for a scanner defect (Issue #25) concerning generic labels (`SETA.`/`SETB.`/`SETC.`) that precede recognised verbs, and a genuinely unrecognised statement under label `SETD.` that must still be flagged as a gap [[MMP9400:1-5]]. Header comments are unverified author prose, so this is *(inferred)* rather than a confirmed business purpose: the source itself contains no comment-independent evidence of what business outcome, if any, the module serves. No business function beyond setting an internal status and calling another program was found in the supplied source.

## How it is invoked

No JCL step, CICS transaction, or program call referring to MMP9400 was found in the ingested set [[MMP9400]]. It may be dead code, invoked dynamically, or started from a scheduler or menu definition that was not supplied *(unresolved — see gap register; confirm before treating anything below as live functionality)*.

## Inputs

| Name | Format | Source | Notes | Citation |
|---|---|---|---|---|

No parameters or context-establishing reads were found in the supplied source.

## Data used

| Data store | Operations | Key / access path | Purpose | Citation |
|---|---|---|---|---|

No database or file access was found in the supplied source.

## Business rules

1. **Status set to confirmed** (MMP9400:BR-001) — The module unconditionally sets its internal `#STATUS` field to `CONF` (business vocabulary: confirmed) [[MMP9400:9]].
2. **Confirmed-status check** (MMP9400:BR-002) — The module tests whether `#STATUS` equals `CONF` [[MMP9400:11]]. What happens when this condition is true cannot be determined from the supplied source: the statement immediately following the condition was not recognised by the scanner *(unresolved — [[MMP9400:13]]; needs SME/source confirmation of the intended action)*.

## Processing sequence

1. Sets `#STATUS` to `CONF` [[MMP9400:9]].
2. Calls `PROGA` via `CALLNAT` [[MMP9400:10]]. Source for `PROGA` was not supplied, so what it does and what it returns are unknown *(unresolved)*.
3. Tests whether `#STATUS` equals `CONF` [[MMP9400:11]] (rule 2 above).
4. Contains a statement at line 13 that the Natural scanner could not recognise; its content and role in the logic are unknown *(unresolved)*.

## Transaction boundaries

No commit (`END TRANSACTION` or equivalent) was found anywhere in the supplied source for this module. Whether any database change is intended to be committed here, or committed elsewhere by a caller, needs SME confirmation — do not assume changes are committed within this module.

## Outputs and effects

- The internal variable `#STATUS` is set to `CONF` [[MMP9400:9]].
- `PROGA` is called [[MMP9400:10]]; any side effects of that call cannot be determined without its source *(unresolved)*.

No database writes, screen output, or messages were found in the supplied source.

## Error handling

No error-handling statements, message text, or `REINPUT` were found in the supplied source for this module.

## Gaps and questions for review

- Is MMP9400 dead code, a dynamically-invoked module, or started from a scheduler/menu not included in this source set? No JCL step, CICS transaction, or program call refers to it [[MMP9400]].
- What statement did the source intend at line 13? The Natural scanner could not recognise it [[MMP9400:13]].
- Is the source for `PROGA` (called via `CALLNAT` at line 10) available, and if so, what does it do? [[MMP9400:10]]